node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# Paraglide compiled output (auto-generated by inlang; also self-ignored via a nested .gitignore).
# Listed here so eslint (includeIgnoreFile) skips it too; regenerated by the `prepare` compile.
/src/lib/paraglide

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Lighthouse CI
.lighthouseci/

# Playwright
test-results/

# Backup files
*.bak

# Editor/tool config
.claude/
.cursor/

# BMAD artifacts — not app source code, exclude from lint/format tooling
_bmad/
_bmad-output/
